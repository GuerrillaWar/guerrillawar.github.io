fontSmoothing(value = 'empty')
  if value == 'empty' || unquote(value) == on
    -webkit-font-smoothing: antialiased;
    -moz-osx-font-smoothing: grayscale;
  else
    -webkit-font-smoothing: subpixel-antialiased;
    -moz-osx-font-smoothing: auto;
